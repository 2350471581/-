# 无障碍 + 单元测试实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 BillTracker 添加 Compose Semantics 无障碍语义支持 + ViewModel/Repository 层单元测试

**Architecture:** 测试使用 MockK mock 依赖 + 真实 `SharedPreferences`（`InMemorySharedPreferences`） + `StandardTestDispatcher` 控制协程。无障碍在每个 Composable 上直接附加 `semantics {}` 属性，不改变视觉布局。

**Tech Stack:** JUnit 4.13.2, MockK 1.13.9, kotlinx-coroutines-test 1.7.3, AndroidX Test, Compose Semantics API

---

### 文件总览

**创建：**
- `app/src/test/java/com/example/billtracker/TestUtils.kt` — 共享测试工具
- `app/src/test/java/com/example/billtracker/data/PlanStorageTest.kt` — PlanStorage 测试
- `app/src/test/java/com/example/billtracker/viewmodel/LedgerViewModelTest.kt` — LedgerViewModel 测试
- `app/src/test/java/com/example/billtracker/viewmodel/PlanViewModelTest.kt` — PlanViewModel 测试
- `app/src/test/java/com/example/billtracker/viewmodel/AnalysisViewModelTest.kt` — AnalysisViewModel 测试
- `app/src/test/java/com/example/billtracker/data/InMemorySharedPreferences.kt` — 测试用 SharedPreferences 实现

**修改（无障碍）：**
- `ui/components/TransactionCard.kt` — 语义合并 + contentDescription
- `ui/components/SourceLabel.kt` — 装饰性图标隐藏
- `ui/components/BottomNavBar.kt` — 选中状态语义
- `ui/components/AddTransactionDialog.kt` — 字段标签
- `ui/components/TransactionDetailCard.kt` — 字段标签
- `ui/MainScreen.kt` — 余额卡片语义、左滑按钮描述
- `ui/SearchScreen.kt` — 搜索框标签
- `ui/AIChatScreen.kt` — 输入框/发送按钮标签
- `ui/AnalysisScreen.kt` — 月份切换、图表区域
- `ui/PlanScreen.kt` — 数字输入框标签
- `ui/ProfileScreen.kt` — 菜单项描述

---

### Task 1: 测试工具基础设施

**Files:**
- Create: `app/src/test/java/com/example/billtracker/data/InMemorySharedPreferences.kt`
- Create: `app/src/test/java/com/example/billtracker/TestUtils.kt`

- [ ] **Step 1: 创建 InMemorySharedPreferences**

```kotlin
package com.example.billtracker.data

import android.content.SharedPreferences
import java.util.*

class InMemorySharedPreferences : SharedPreferences {
    private val map = mutableMapOf<String, Any?>()
    private val listeners = mutableSetOf<SharedPreferences.OnSharedPreferenceChangeListener>()

    override fun getAll(): Map<String, *> = map.toMap()
    override fun getString(key: String, defValue: String?) = map[key] as? String ?: defValue
    override fun getStringSet(key: String, defValue: MutableSet<String>?) = map[key] as? MutableSet<String> ?: defValue
    override fun getInt(key: String, defValue: Int) = (map[key] as? Int) ?: defValue
    override fun getLong(key: String, defValue: Long) = (map[key] as? Long) ?: defValue
    override fun getFloat(key: String, defValue: Float) = (map[key] as? Float) ?: defValue
    override fun getBoolean(key: String, defValue: Boolean) = (map[key] as? Boolean) ?: defValue
    override fun contains(key: String) = map.containsKey(key)

    override fun edit(): SharedPreferences.Editor = InMemoryEditor()
    override fun registerOnSharedPreferenceChangeListener(l: SharedPreferences.OnSharedPreferenceChangeListener) { listeners.add(l) }
    override fun unregisterOnSharedPreferenceChangeListener(l: SharedPreferences.OnSharedPreferenceChangeListener) { listeners.remove(l) }

    inner class InMemoryEditor : SharedPreferences.Editor {
        private val tempMap = mutableMapOf<String, Any?>()
        private val removeSet = mutableSetOf<String>()

        override fun putString(key: String, value: String?) = apply { tempMap[key] = value }
        override fun putStringSet(key: String, value: MutableSet<String>?) = apply { tempMap[key] = value }
        override fun putInt(key: String, value: Int) = apply { tempMap[key] = value }
        override fun putLong(key: String, value: Long) = apply { tempMap[key] = value }
        override fun putFloat(key: String, value: Float) = apply { tempMap[key] = value }
        override fun putBoolean(key: String, value: Boolean) = apply { tempMap[key] = value }
        override fun remove(key: String) = apply { removeSet.add(key) }
        override fun clear() = apply { tempMap.clear(); removeSet.clear() }

        override fun commit(): Boolean {
            apply()
            return true
        }

        override fun apply() {
            map.putAll(tempMap)
            removeSet.forEach { map.remove(it) }
            listeners.forEach { l -> tempMap.keys.forEach { k -> l.onSharedPreferenceChanged(this@InMemorySharedPreferences, k) } }
        }
    }
}
```

- [ ] **Step 2: 创建 TestUtils.kt**

```kotlin
package com.example.billtracker

import com.example.billtracker.data.TransactionEntity
import com.example.billtracker.data.TransactionSource
import com.example.billtracker.data.TransactionType

fun createTestTransaction(
    id: Long = 0,
    amount: Double = 100.0,
    type: TransactionType = TransactionType.EXPENSE,
    source: TransactionSource = TransactionSource.MANUAL,
    description: String = "测试账单",
    dateMillis: Long = System.currentTimeMillis(),
    category: String = "其他"
) = TransactionEntity(
    id = id, amount = amount, type = type, source = source,
    description = description, dateMillis = dateMillis, category = category
)
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugUnitTestSources`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/test/java/com/example/billtracker/data/InMemorySharedPreferences.kt app/src/test/java/com/example/billtracker/TestUtils.kt
git commit -m "test: add in-memory SharedPreferences and test utilities"
```

---

### Task 2: PlanStorageTest

**Files:**
- Create: `app/src/test/java/com/example/billtracker/data/PlanStorageTest.kt`

- [ ] **Step 1: 编写 PlanStorageTest**

```kotlin
package com.example.billtracker.data

import com.google.common.truth.Truth.assertThat
import org.junit.Before
import org.junit.Test

class PlanStorageTest {
    private lateinit var storage: PlanStorage

    @Before
    fun setup() {
        storage = PlanStorage(InMemorySharedPreferences())
    }

    @Test
    fun `balance defaults to 0`() {
        assertThat(storage.balance).isEqualTo(0.0)
    }

    @Test
    fun `balance round-trip`() {
        storage.balance = 1234.56
        assertThat(storage.balance).isEqualTo(1234.56)
    }

    @Test
    fun `todayPlanTarget round-trip`() {
        storage.todayPlanTarget = 500.0
        assertThat(storage.todayPlanTarget).isEqualTo(500.0)
    }

    @Test
    fun `totalPlanTarget round-trip`() {
        storage.totalPlanTarget = 10000.0
        assertThat(storage.totalPlanTarget).isEqualTo(10000.0)
    }

    @Test
    fun `savePlanTarget round-trip`() {
        storage.savePlanTarget = 2000.0
        assertThat(storage.savePlanTarget).isEqualTo(2000.0)
    }

    @Test
    fun `string fields round-trip`() {
        storage.todayPlanNote = "今日目标"
        assertThat(storage.todayPlanNote).isEqualTo("今日目标")
        storage.totalPlanNote = "总目标"
        assertThat(storage.totalPlanNote).isEqualTo("总目标")
        storage.savePlanNote = "存款目标"
        assertThat(storage.savePlanNote).isEqualTo("存款目标")
    }

    @Test
    fun `nickname defaults to user`() {
        assertThat(storage.nickname).isEqualTo("用户")
    }

    @Test
    fun `nickname round-trip`() {
        storage.nickname = "测试用户"
        assertThat(storage.nickname).isEqualTo("测试用户")
    }

    @Test
    fun `themeIndex defaults to 2`() {
        assertThat(storage.themeIndex).isEqualTo(2)
    }

    @Test
    fun `isManualMode defaults to true`() {
        assertThat(storage.isManualMode).isTrue()
    }

    @Test
    fun `customPlan CRUD`() {
        val plan = CustomPlan("旅行基金", 5000.0, "年底旅行", PlanDataType.TOTAL_NET)
        storage.addCustomPlan(plan)

        val plans = storage.getAllCustomPlans()
        assertThat(plans).hasSize(1)
        assertThat(plans[0].name).isEqualTo("旅行基金")
        assertThat(plans[0].target).isEqualTo(5000.0)

        storage.updateCustomPlan(0, 6000.0, "欧洲旅行")
        val updated = storage.getCustomPlan(0)!!
        assertThat(updated.target).isEqualTo(6000.0)
        assertThat(updated.note).isEqualTo("欧洲旅行")

        storage.deleteCustomPlan(0)
        assertThat(storage.getAllCustomPlans()).isEmpty()
    }

    @Test
    fun `installDateMillis is stable`() {
        val date1 = storage.installDateMillis
        val date2 = storage.installDateMillis
        assertThat(date2).isEqualTo(date1)
    }

    @Test
    fun `aiChatEnabled round-trip`() {
        storage.aiChatEnabled = true
        assertThat(storage.aiChatEnabled).isTrue()
        storage.aiChatEnabled = false
        assertThat(storage.aiChatEnabled).isFalse()
    }

    @Test
    fun `customThemeConfigJson round-trip`() {
        storage.customThemeConfigJson = """{"imageUri":"test"}"""
        assertThat(storage.customThemeConfigJson).isEqualTo("""{"imageUri":"test"}""")
    }

    @Test
    fun `deleteCustomPlan out of bounds does nothing`() {
        storage.deleteCustomPlan(99)
        assertThat(storage.getAllCustomPlans()).isEmpty()
    }
}
```

- [ ] **Step 2: 运行测试**

Run: `./gradlew :app:testDebugUnitTest --tests "com.example.billtracker.data.PlanStorageTest"`
Expected: 16/16 passed

- [ ] **Step 3: Commit**

```bash
git add app/src/test/java/com/example/billtracker/data/PlanStorageTest.kt
git commit -m "test: add PlanStorage unit tests"
```

---

### Task 3: LedgerViewModelTest

**Files:**
- Create: `app/src/test/java/com/example/billtracker/viewmodel/LedgerViewModelTest.kt`

- [ ] **Step 1: 编写 LedgerViewModelTest**

```kotlin
package com.example.billtracker.viewmodel

import android.app.Application
import androidx.arch.core.executor.testing.InstantTaskExecutorRule
import com.example.billtracker.createTestTransaction
import com.example.billtracker.data.InMemorySharedPreferences
import com.example.billtracker.data.PlanStorage
import com.example.billtracker.data.TransactionDao
import com.example.billtracker.data.TransactionRepository
import com.example.billtracker.data.TransactionType
import io.mockk.coEvery
import io.mockk.coVerify
import io.mockk.every
import io.mockk.mockk
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.flowOf
import kotlinx.coroutines.test.StandardTestDispatcher
import kotlinx.coroutines.test.resetMain
import kotlinx.coroutines.test.runTest
import kotlinx.coroutines.test.setMain
import org.junit.After
import org.junit.Before
import org.junit.Rule
import org.junit.Test

class LedgerViewModelTest {

    @get:Rule val instantTaskRule = InstantTaskExecutorRule()

    private val testDispatcher = StandardTestDispatcher()
    private val app = mockk<Application>(relaxed = true)
    private val dao = mockk<TransactionDao>()
    private val repo = mockk<TransactionRepository>()

    private lateinit var viewModel: LedgerViewModel

    @Before
    fun setup() {
        Dispatchers.setMain(testDispatcher)
        every { dao.getTransactionsBetweenSync(any(), any()) } returns emptyList()
        every { repo.getTodayTransactions(any(), any()) } returns flowOf(emptyList())
        every { repo.getTodayIncome(any(), any()) } returns flowOf(0.0)
        every { repo.getTodayExpense(any(), any()) } returns flowOf(0.0)
        every { repo.getAllTransactions() } returns flowOf(emptyList())
    }

    @After
    fun teardown() {
        Dispatchers.resetMain()
    }

    private fun createViewModel(): LedgerViewModel {
        val planStorage = PlanStorage(InMemorySharedPreferences())
        return LedgerViewModel(repo, planStorage, dao, mockk(relaxed = true), app)
    }

    @Test
    fun `addTransaction calls repository`() = runTest {
        viewModel = createViewModel()
        coEvery { repo.addManualTransaction(35.0, TransactionType.EXPENSE, "午餐") } returns true

        var result = false
        viewModel.addTransaction(35.0, TransactionType.EXPENSE, "午餐") { result = it }
        testDispatcher.scheduler.advanceUntilIdle()

        coVerify { repo.addManualTransaction(35.0, TransactionType.EXPENSE, "午餐") }
        assert(result) { "addTransaction should report success" }
    }

    @Test
    fun `addTransaction reports duplicate`() = runTest {
        viewModel = createViewModel()
        coEvery { repo.addManualTransaction(any(), any(), any()) } returns false

        var result = true
        viewModel.addTransaction(50.0, TransactionType.INCOME, "工资") { result = it }
        testDispatcher.scheduler.advanceUntilIdle()

        assert(!result) { "addTransaction should report false for duplicates" }
    }

    @Test
    fun `deleteTransaction calls repository`() = runTest {
        viewModel = createViewModel()
        coEvery { repo.deleteTransaction(1L) } returns Unit

        viewModel.deleteTransaction(1L)
        testDispatcher.scheduler.advanceUntilIdle()

        coVerify { repo.deleteTransaction(1L) }
    }

    @Test
    fun `getTodaySummary returns formatted string`() = runTest {
        val now = System.currentTimeMillis()
        val tx = listOf(
            createTestTransaction(amount = 35.0, type = TransactionType.EXPENSE, description = "午餐", dateMillis = now),
            createTestTransaction(amount = 200.0, type = TransactionType.INCOME, description = "退款", dateMillis = now)
        )
        every { dao.getTransactionsBetweenSync(any(), any()) } returns tx

        viewModel = createViewModel()
        val summary = viewModel.getTodaySummary()

        assert(summary.contains("收入")) { "summary should contain income info" }
        assert(summary.contains("支出")) { "summary should contain expense info" }
        assert(summary.contains("¥200.00")) { "summary should show income amount" }
        assert(summary.contains("¥35.00")) { "summary should show expense amount" }
    }

    @Test
    fun `setManualMode updates state and storage`() {
        viewModel = createViewModel()

        viewModel.setManualMode(true)
        assert(viewModel.isManualMode.value)

        viewModel.setManualMode(false)
        assert(!viewModel.isManualMode.value)
    }

    @Test
    fun `installDateMillis is stable after ViewModel init`() {
        viewModel = createViewModel()
        assert(viewModel.installDateMillis > 0)
    }
}
```

- [ ] **Step 2: 运行测试**

Run: `./gradlew :app:testDebugUnitTest --tests "com.example.billtracker.viewmodel.LedgerViewModelTest"`
Expected: 6/6 passed

- [ ] **Step 3: Commit**

```bash
git add app/src/test/java/com/example/billtracker/viewmodel/LedgerViewModelTest.kt
git commit -m "test: add LedgerViewModel unit tests"
```

---

### Task 4: PlanViewModelTest

**Files:**
- Create: `app/src/test/java/com/example/billtracker/viewmodel/PlanViewModelTest.kt`

- [ ] **Step 1: 编写 PlanViewModelTest**

```kotlin
package com.example.billtracker.viewmodel

import androidx.arch.core.executor.testing.InstantTaskExecutorRule
import com.example.billtracker.data.InMemorySharedPreferences
import com.example.billtracker.data.PlanDataType
import com.example.billtracker.data.PlanStorage
import org.junit.Before
import org.junit.Rule
import org.junit.Test

class PlanViewModelTest {

    @get:Rule val instantTaskRule = InstantTaskExecutorRule()

    private lateinit var viewModel: PlanViewModel

    @Before
    fun setup() {
        val storage = PlanStorage(InMemorySharedPreferences())
        viewModel = PlanViewModel(storage)
    }

    @Test
    fun `planBalance defaults to 0`() {
        assert(viewModel.planBalance.value == 0.0)
    }

    @Test
    fun `updatePlanBalance updates value`() {
        viewModel.updatePlanBalance(5000.0)
        assert(viewModel.planBalance.value == 5000.0)
    }

    @Test
    fun `todayPlanTarget round-trip`() {
        viewModel.updateTodayPlanTarget(300.0)
        assert(viewModel.todayPlanTarget.value == 300.0)
    }

    @Test
    fun `totalPlanTarget round-trip`() {
        viewModel.updateTotalPlanTarget(10000.0)
        assert(viewModel.totalPlanTarget.value == 10000.0)
    }

    @Test
    fun `savePlanTarget round-trip`() {
        viewModel.updateSavePlanTarget(2000.0)
        assert(viewModel.savePlanTarget.value == 2000.0)
    }

    @Test
    fun `todayPlanNote round-trip`() {
        viewModel.updateTodayPlanNote("今日目标")
        assert(viewModel.todayPlanNote.value == "今日目标")
    }

    @Test
    fun `totalPlanNote round-trip`() {
        viewModel.updateTotalPlanNote("总目标")
        assert(viewModel.totalPlanNote.value == "总目标")
    }

    @Test
    fun `addCustomPlan adds to list`() {
        viewModel.addCustomPlan("旅行基金", 5000.0, "年底旅行")
        assert(viewModel.customPlans.value.size == 1)
        assert(viewModel.customPlans.value[0].name == "旅行基金")
    }

    @Test
    fun `updateCustomPlan modifies existing plan`() {
        viewModel.addCustomPlan("旅行基金", 5000.0, "年底旅行")
        viewModel.updateCustomPlan(0, 6000.0, "欧洲旅行")
        assert(viewModel.customPlans.value[0].target == 6000.0)
        assert(viewModel.customPlans.value[0].note == "欧洲旅行")
    }

    @Test
    fun `deleteCustomPlan removes plan`() {
        viewModel.addCustomPlan("旅行基金", 5000.0, "年底旅行")
        viewModel.deleteCustomPlan(0)
        assert(viewModel.customPlans.value.isEmpty())
    }

    @Test
    fun `multiple custom plans maintain order`() {
        viewModel.addCustomPlan("计划A", 100.0, "A")
        viewModel.addCustomPlan("计划B", 200.0, "B")
        viewModel.addCustomPlan("计划C", 300.0, "C")
        assert(viewModel.customPlans.value.size == 3)
        assert(viewModel.customPlans.value[1].name == "计划B")

        viewModel.deleteCustomPlan(1)
        assert(viewModel.customPlans.value.size == 2)
        assert(viewModel.customPlans.value[1].name == "计划C")
    }
}
```

- [ ] **Step 2: 运行测试**

Run: `./gradlew :app:testDebugUnitTest --tests "com.example.billtracker.viewmodel.PlanViewModelTest"`
Expected: 11/11 passed

- [ ] **Step 3: Commit**

```bash
git add app/src/test/java/com/example/billtracker/viewmodel/PlanViewModelTest.kt
git commit -m "test: add PlanViewModel unit tests"
```

---

### Task 5: AnalysisViewModelTest

**Files:**
- Create: `app/src/test/java/com/example/billtracker/viewmodel/AnalysisViewModelTest.kt`

- [ ] **Step 1: 编写 AnalysisViewModelTest**

```kotlin
package com.example.billtracker.viewmodel

import androidx.arch.core.executor.testing.InstantTaskExecutorRule
import com.example.billtracker.data.DeepSeekService
import io.mockk.coEvery
import io.mockk.coVerify
import io.mockk.mockk
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.flowOf
import kotlinx.coroutines.test.StandardTestDispatcher
import kotlinx.coroutines.test.resetMain
import kotlinx.coroutines.test.runTest
import kotlinx.coroutines.test.setMain
import org.junit.After
import org.junit.Before
import org.junit.Rule
import org.junit.Test

class AnalysisViewModelTest {

    @get:Rule val instantTaskRule = InstantTaskExecutorRule()

    private val testDispatcher = StandardTestDispatcher()
    private val deepSeekService = mockk<DeepSeekService>()
    private val repo = mockk<com.example.billtracker.data.TransactionRepository>()

    private lateinit var viewModel: AnalysisViewModel

    @Before
    fun setup() {
        Dispatchers.setMain(testDispatcher)
        every { repo.getAllTransactions() } returns flowOf(emptyList())
        viewModel = AnalysisViewModel(repo, deepSeekService)
    }

    @After
    fun teardown() {
        Dispatchers.resetMain()
    }

    @Test
    fun `generateMonthlySummary returns text on success`() = runTest {
        val expected = "2024年3月总结"
        coEvery { deepSeekService.generateMonthlySummary(2024, 3, 5000.0, 3000.0, 2000.0, any(), 10, "") } returns expected

        val result = viewModel.generateMonthlySummary(2024, 3, 5000.0, 3000.0, 2000.0, mapOf("餐饮" to 1000.0), 10)

        assert(result == expected)
        coVerify { deepSeekService.generateMonthlySummary(2024, 3, 5000.0, 3000.0, 2000.0, any(), 10, "") }
    }

    @Test
    fun `generateMonthlySummary returns empty on failure`() = runTest {
        coEvery { deepSeekService.generateMonthlySummary(any(), any(), any(), any(), any(), any(), any(), any()) } returns ""

        val result = viewModel.generateMonthlySummary(2024, 3, 0.0, 0.0, 0.0, emptyMap(), 0)

        assert(result == "")
    }

    @Test
    fun `allTransactions reflects repo data`() {
        every { repo.getAllTransactions() } returns flowOf(
            listOf(com.example.billtracker.createTestTransaction(id = 1))
        )
        viewModel = AnalysisViewModel(repo, deepSeekService)
        assert(viewModel.allTransactions.value.size == 1)
    }
}
```

- [ ] **Step 2: 运行所有测试确认通过**

Run: `./gradlew :app:testDebugUnitTest`
Expected: 36/36 passed (16 PlanStorage + 6 LedgerVM + 11 PlanVM + 3 AnalysisVM)

- [ ] **Step 3: Commit**

```bash
git add app/src/test/java/com/example/billtracker/viewmodel/AnalysisViewModelTest.kt
git commit -m "test: add AnalysisViewModel unit tests"
```

---

### Task 6: 无障碍 — TransactionCard / SourceLabel

**Files:**
- Modify: `ui/components/TransactionCard.kt`
- Modify: `ui/components/SourceLabel.kt`

- [ ] **Step 1: SourceLabel — 标记为装饰性**

```kotlin
@Composable
fun SourceLabel(
    source: TransactionSource,
    modifier: Modifier = Modifier
) {
    val color = sourceColor(source)
    Surface(
        shape = RoundedCornerShape(4.dp),
        color = color.copy(alpha = 0.12f),
        modifier = modifier.semantics(mergeDescendants = true) { }
    ) {
        Text(
            text = sourceDisplayName(source),
            modifier = Modifier.padding(horizontal = 6.dp, vertical = 2.dp),
            fontSize = 10.sp,
            fontWeight = FontWeight.Medium,
            color = color
        )
    }
}
```

Add import: `import androidx.compose.ui.semantics.semantics`

- [ ] **Step 2: TransactionCard — 合并语义节点**

```kotlin
import androidx.compose.ui.semantics.contentDescription
import androidx.compose.ui.semantics.invisibleToUser
import androidx.compose.ui.semantics.semantics
```

At the top of the composable body, before the Card, compute a11y description:
```kotlin
val typeLabel = when (transaction.type) {
    TransactionType.INCOME -> "收入"
    TransactionType.EXPENSE -> "支出"
}
val sourceName = sourceDisplayName(transaction.source)
val a11yDescription = "${typeLabel}，¥${"%.2f".format(transaction.amount)}，${transaction.category}，${sourceName}"
```

On the Card, add semantics:
```kotlin
Card(
    modifier = modifier.fillMaxWidth().semantics {
        contentDescription = a11yDescription
    },
    ...
)
```

On the inner Row, add semantics to mark children invisible:
```kotlin
Row(
    modifier = Modifier
        .fillMaxWidth()
        .height(if (compactMode) 56.dp else 64.dp)
        .semantics { invisibleToUser() },
    ...
)
```

Import: `import com.example.billtracker.ui.components.sourceDisplayName`

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/components/TransactionCard.kt app/src/main/java/com/example/billtracker/ui/components/SourceLabel.kt
git commit -m "a11y: add semantics to TransactionCard and SourceLabel"
```

---

### Task 7: 无障碍 — BottomNavBar + 余额卡片

**Files:**
- Modify: `ui/components/BottomNavBar.kt`
- Modify: `ui/MainScreen.kt`

- [ ] **Step 1: BottomNavBar — 选中状态语义**

In `BottomNavBar.kt`, add import:
```kotlin
import androidx.compose.ui.semantics.stateDescription
import androidx.compose.ui.semantics.semantics
```

In `BillBottomNavItem`, add semantics to the Column:
```kotlin
Column(
    modifier = Modifier
        .clip(RoundedCornerShape(12.dp))
        .clickable { onClick() }
        .padding(horizontal = 20.dp, vertical = 6.dp)
        .semantics {
            stateDescription = if (isSelected) "已选中" else "未选中"
        },
    ...
)
```

- [ ] **Step 2: 余额卡片 — 合并语义**

In `MainScreen.kt`, add imports:
```kotlin
import androidx.compose.ui.semantics.contentDescription
import androidx.compose.ui.semantics.clearAndSetSemantics
```

In `WeChatStyleBalanceCard`, at the beginning of the composable:
```kotlin
val incomeLabel = "收入 ¥${"%.2f".format(income)}"
val expenseLabel = "支出 ¥${"%.2f".format(expense)}"
val netLabel = if (net >= 0) "净收入 ¥${"%.2f".format(net)}" else "净支出 ¥${"%.2f".format(-net)}"
val a11yBalanceDesc = "$title：$netLabel，$incomeLabel，$expenseLabel"
```

Add semantics to the outer Card:
```kotlin
Card(
    modifier = modifier.fillMaxWidth()
        .clearAndSetSemantics { contentDescription = a11yBalanceDesc },
    ...
)
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/components/BottomNavBar.kt app/src/main/java/com/example/billtracker/ui/MainScreen.kt
git commit -m "a11y: add semantics to BottomNavBar and WeChatStyleBalanceCard"
```

---

### Task 8: 无障碍 — 弹窗（添加账单/交易详情）

**Files:**
- Modify: `ui/components/AddTransactionDialog.kt`
- Modify: `ui/components/TransactionDetailCard.kt`

- [ ] **Step 1: AddTransactionDialog — 字段标签**

Add import:
```kotlin
import androidx.compose.ui.semantics.contentDescription
```

OutlinedTextField for amount:
```kotlin
OutlinedTextField(
    ...
    modifier = Modifier.fillMaxWidth().semantics { contentDescription = "金额输入框" },
    ...
)
```

OutlinedTextField for note:
```kotlin
OutlinedTextField(
    ...
    modifier = Modifier.fillMaxWidth().semantics { contentDescription = "备注输入框" },
    ...
)
```

FilterChips for type:
```kotlin
FilterChip(
    ...
    modifier = Modifier.weight(1f).semantics { contentDescription = "支出类型" },
    ...
)
FilterChip(
    ...
    modifier = Modifier.weight(1f).semantics { contentDescription = "收入类型" },
    ...
)
```

Confirm button — add description:
```kotlin
Button(
    onClick = { ... },
    modifier = Modifier.semantics { contentDescription = "添加账单" },
    ...
)
```

Import: `import androidx.compose.ui.semantics.contentDescription`
Import: `import androidx.compose.ui.semantics.semantics`

- [ ] **Step 2: TransactionDetailCard — 语义合并 + 备注编辑框标签**

In `TransactionDetailCard.kt`, add imports:
```kotlin
import androidx.compose.ui.semantics.contentDescription
import androidx.compose.ui.semantics.semantics
```

On the outer Dialog Card, add semantics to merge the header info:
```kotlin
Card(
    modifier = Modifier
        .fillMaxWidth(0.9f)
        .fillMaxHeight(0.55f)
        .semantics { contentDescription = "交易详情：$sourceLabel $typeLabel $typePrefix¥${"%.2f".format(transaction.amount)}" },
    ...
)
```

The OutlinedTextField for notes:
```kotlin
OutlinedTextField(
    value = notesInput,
    onValueChange = { notesInput = it },
    placeholder = { Text("添加备注...") },
    modifier = Modifier
        .fillMaxWidth()
        .weight(1f)
        .semantics { contentDescription = "备注编辑框" },
    ...
)
```

- [ ] **Step 4: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/components/AddTransactionDialog.kt app/src/main/java/com/example/billtracker/ui/components/TransactionDetailCard.kt
git commit -m "a11y: add semantics to AddTransactionDialog and TransactionDetailCard"
```

---

### Task 9: 无障碍 — SearchScreen + AIChatScreen

**Files:**
- Modify: `ui/SearchScreen.kt`
- Modify: `ui/AIChatScreen.kt`

- [ ] **Step 1: SearchScreen — 搜索框标签**

Add import:
```kotlin
import androidx.compose.ui.semantics.contentDescription
import androidx.compose.ui.semantics.semantics
```

Search OutlinedTextField — add semantics:
```kotlin
OutlinedTextField(
    ...
    modifier = Modifier
        .fillMaxWidth()
        .padding(horizontal = 16.dp, vertical = 8.dp)
        .focusRequester(focusRequester)
        .semantics { contentDescription = "搜索账单描述、分类或金额" },
    ...
)
```

Clear/close icon button already has `contentDescription = "清除"` — change to "清除搜索内容":
```kotlin
IconButton(onClick = { query = "" }) {
    Icon(
        Icons.Default.Close,
        contentDescription = "清除搜索内容",
        ...
    )
}
```

- [ ] **Step 2: AIChatScreen — 输入框标签**

Add import to `AIChatScreen.kt`:
```kotlin
import androidx.compose.ui.semantics.contentDescription
```

The OutlinedTextField already exists with `placeholder = { Text("输入收支描述...") }`. Add semantics:
```kotlin
OutlinedTextField(
    ...
    modifier = Modifier.weight(1f).semantics { contentDescription = "输入记账内容" },
    ...
)
```

The send FilledIconButton already has `contentDescription = "发送"` on the Icon — keep as-is.

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/SearchScreen.kt app/src/main/java/com/example/billtracker/ui/AIChatScreen.kt
git commit -m "a11y: add semantics to SearchScreen and AIChatScreen"
```

---

### Task 10: 无障碍 — AnalysisScreen + PlanScreen

**Files:**
- Modify: `ui/AnalysisScreen.kt`
- Modify: `ui/PlanScreen.kt`

- [ ] **Step 1: AnalysisScreen — 月份切换 + 图表区域**

Add imports:
```kotlin
import androidx.compose.ui.semantics.contentDescription
import androidx.compose.ui.semantics.semantics
```

Find the month navigation buttons (left/right arrows) and add:
```kotlin
.semantics { contentDescription = "上一个月" }
```
```kotlin
.semantics { contentDescription = "下一个月" }
```

Find the chart area (likely a `Box` or `Canvas`) and add:
```kotlin
.semantics { contentDescription = "本月支出趋势图" }
```

- [ ] **Step 2: PlanScreen — 数字输入框标签**

Find all `OutlinedTextField` for balance/target inputs and add `contentDescription` semantics. Example:
```kotlin
.semantics { contentDescription = "计划余额输入" }
.semantics { contentDescription = "今日目标输入" }
.semantics { contentDescription = "总目标输入" }
.semantics { contentDescription = "存款目标输入" }
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/AnalysisScreen.kt app/src/main/java/com/example/billtracker/ui/PlanScreen.kt
git commit -m "a11y: add semantics to AnalysisScreen and PlanScreen"
```

---

### Task 11: 无障碍 — 其余屏幕

**Files:**
- Modify: `ui/MainScreen.kt` — TransactionItem 左滑操作按钮、PrivacyFooter

- [ ] **Step 1: PrivacyFooter — 合并为单行语义**

Find `PrivacyFooter()` composable and add:
```kotlin
.semantics { contentDescription = "你的隐私数据仅保存在本地，不会上传" }
```

- [ ] **Step 2: TransactionItem 左滑按钮 — 补充一致性描述**

The "详情" surface button — add contentDescription for the icon:
```kotlin
Icon(
    Icons.AutoMirrored.Filled.Message,
    contentDescription = "查看详情",
    ...
)
```

The "删除" surface button — already has text label. The delete icon:
```kotlin
Icon(
    Icons.Default.Delete,
    contentDescription = "删除",
    ...
)
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 运行全部测试确认未破坏**

Run: `./gradlew :app:testDebugUnitTest`
Expected: 36/36 passed

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/example/billtracker/ui/MainScreen.kt
git commit -m "a11y: add semantics to TransactionItem and PrivacyFooter"
```
